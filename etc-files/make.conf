# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
CFLAGS="-march=core-avx-i -mtune=native -O2 -pipe"
CXXFLAGS="${CFLAGS}"
MAKEOPTS="-j6"
# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.
CHOST="x86_64-pc-linux-gnu"
# These are the USE flags that were used in addition to what is provided by the
# profile used for building.
USE="-consolekit -gtk -gnome -gnome-keyring -qt4 systemd udev wayland X qt5 alsa dvd cdr mmx sse sse2 acpi apm bash-completion bluetooth connman cups dri vaapi emacs ffmpeg flac fontconfig matroska multilib opengl pulseaudio session sound truetype corefonts cleartype dbus"
DISTDIR="${PORTDIR}/distfiles"
PKGDIR="${PORTDIR}/packages"
ACCEPT_KEYWORDS="~amd64"
LCD_DEVICES=""
VIDEO_CARDS="intel i965 radeon"
GPSD_PROTOCOLS=""
APACHE2_MODULES=""
INPUT_DEVICES="evdev wacom"
LINGUAS="en_GB ro"
#FEATURES="ccache"
#CCACHE_DIR="/home/adi/ccache"
#CCACHE_SIZE="10G"
#CCACHE_UMASK=002

EGIT3_STORE_DIR="/home/adi/workspace/portage-git-sources"
EGIT_CLONE_TYPE="single"
EGIT_MIN_CLONE_TYPE="single"

CALLIGRA_FEATURES="krita"
